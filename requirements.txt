# The Python side of the pipeline, fully pinned.
#
# A full pin rather than the two direct dependencies (mlx-audio[stt], mlx-lm),
# because the measurements in CLAUDE.md were taken against these versions and a
# floating transitive dependency would silently move them.
#
# Every package here has an arm64 wheel for the interpreter named by
# runtime::PYTHON_VERSION, so a bootstrap is a download and never a compile.
#
# Regenerate with: uv pip freeze | sort

annotated-doc==0.0.5
anyio==4.14.2
certifi==2026.7.22
cffi==2.1.1
click==8.4.2
filelock==3.32.3
fsspec==2026.7.0
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==1.27.0
idna==3.18
Jinja2==3.1.6
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
miniaudio==1.71
mlx==0.32.0
mlx-audio==0.4.8
mlx-lm==0.31.3
mlx-metal==0.32.0
numpy==2.5.2
packaging==26.3
protobuf==7.35.1
pycparser==3.0
Pygments==2.20.0
PyYAML==6.0.3
regex==2026.7.19
rich==15.0.0
safetensors==0.8.0
scipy==1.18.0
sentencepiece==0.2.2
shellingham==1.5.4
sounddevice==0.5.5
tokenizers==0.22.2
tqdm==4.70.0
transformers==5.15.0
typer==0.27.1
typing_extensions==4.16.0
